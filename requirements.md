# 项目需求文档：AccountKeeper 本地记账软件

## 1. 项目概述
使用 Python 编写一个带图形界面（GUI）的本地记账小工具。该工具需操作简单、界面友好，通过图形化交互完成记账、查账、统计等功能，数据必须持久化保存在本地。

## 2. 技术限制
- **语言**：Python 3.13。
- **依赖**：必须使用 `customtkinter` 库作为 GUI 基础框架，使用 `Pillow` (PIL) 库处理图片，使用 `matplotlib` 库处理图表，使用 `platformdirs` 库管理用户数据目录。其他尽量只使用 Python 标准库（如 `sqlite3`, `csv`, `datetime`, `pathlib`, `json`, `random`）。
- **存储**：默认使用本地 SQLite 数据库文件 `account.db` 存储数据，但实际数据库路径允许用户通过设置界面自定义。用户配置持久化到 `settings.json`。
- **配置**：新增 `settings.json`，用于保存用户选择的数据库文件路径和 CSV 默认目录。程序启动时必须先读取该配置；配置缺失、损坏或非法时，自动回退默认路径，不能崩溃。
- **规范**：代码必须符合 PEP 8 规范，命名清晰，关键逻辑必须配有中文注释。

## 3. 核心功能需求

### 3.1 数据持久化（SQLite 数据库）
- **数据目录**：使用 `platformdirs.user_data_dir("AccountKeeper", "SpringU26325")` 获取用户数据目录（Windows 下通常为 `C:\Users\用户名\AppData\Local\AccountKeeper`），并在启动时自动创建该目录。
- `account.db` 和 `account.csv` 都存放在该用户数据目录下，**绝对不允许**放在临时解压目录或程序安装目录。
- 启动时自动连接 `account.db`。如果数据库或表不存在，自动创建。
- 数据表结构必须包含：ID（自增主键）、日期、金额、类别、备注。
- 每次修改记录（添加、删除）后，必须**立刻提交事务（commit）**，防止数据丢失。
- 推荐使用 `contextmanager` 封装数据库连接，确保连接稳定关闭。

### 3.2 添加记录 (Add)
- 提示用户输入：日期（默认今天，格式 `YYYY-MM-DD`）、金额、类别、备注（可选）。
- **金额输入区使用【支出 / 收入】切换按钮**（推荐 `CTkSegmentedButton` 或两个单选框），默认选中【支出】。
- 用户始终输入正数。系统在保存时，根据选中的状态进行转换：
  - 如果选择【支出】，自动将金额转为**负数**。
  - 如果选择【收入】，保持**正数**。
- 金额不能为 0。如果用户输入非数字，需弹出错误提示框。
- 每条记录自动生成唯一的 ID（自增主键）。

### 3.3 查看记录 (View)
- 使用 `customtkinter` 搭配 `ttk.Treeview` 控件以表格形式展示所有记录。
- 表格需包含滚动条，列头包括：ID、日期、金额、类别、备注。
- 表格上方需有总记录数、总收入、总支出的汇总标签。
- **【UI修复】**：汇总标签必须完整显示，不能被窗口右侧边缘截断。可以缩短文字格式（如 `收: 123.00 | 支: 0.00`）或调整布局留出空间。

### 3.4 统计分析 (Stats)
- 点击按钮弹出输入框，要求用户输入月份（格式 `YYYY-MM`）。
- 统计该月的总支出和总收入，并列出该月各分类的支出明细。
- 如果没有支出记录，给出友好的文字提示。

### 3.5 删除记录 (Delete)
- 选中表格中的某条记录，点击【删除选中记录】按钮。
- 弹出确认对话框，询问“确认删除该记录吗？”（使用 GUI 原生弹窗）。
- 如果 ID 不存在，提示用户“找不到该记录”。

### 3.5.1 编辑记录 (Edit)
- **触发方式**：双击表格中的某一行记录。
- **弹窗内容**：弹出一个编辑对话框，包含日期、金额、类别、备注四个输入框，**必须预填当前记录的旧值**。
- **数据校验**：与“添加记录”保持一致（日期格式 `YYYY-MM-DD`，金额不能为 0 且必须为数字，类别不能为空）。
- **保存逻辑**：
  - 在 `AccountStore` 类中新增 `update` 方法，接收 `record_id` 和四个新字段，执行 SQL `UPDATE accounts SET ... WHERE id = ?`。
  - **必须使用 `contextmanager` 的连接，并在事务成功后 `commit`**。
  - 更新成功后，调用 `refresh_records()` 刷新表格。
- **取消操作**：用户点击“取消”或按 `ESC` 键，关闭弹窗，不修改任何数据。
- **数据一致性**：编辑过程中，不得修改该记录的 ID（ID 是数据库主键，永远不变）。

### 3.6 导出为 CSV (Export)
- 在界面上添加一个【导出为CSV】按钮。
- 点击后弹出输入框，让用户输入想要导出的月份（格式 `YYYY-MM`），复用现有的 `ask_month` 方法。
- 如果该月没有记录，弹出提示“该月没有记录，无法导出”，流程结束。
- **选择保存路径（核心改动）**：
  - 用户输入月份并确认后，调用 `tkinter.filedialog.asksaveasfilename()` 弹出 Windows 原生“另存为”对话框。
  - 参数设置：
    - `defaultextension=".csv"`
    - `initialfile=f"account_export_{month}.csv"`
    - `initialdir=str(csv_dir)`，其中 `csv_dir` 是用户在设置中指定的 CSV 默认目录。
    - `filetypes=[("CSV 文件", "*.csv"), ("所有文件", "*.*")]`
  - 如果用户点击“取消”（返回空字符串），直接终止导出，不报错、不弹窗。
- **写入逻辑（在 `store.py`）**：
  - `export_month_csv` 方法签名保持 `def export_month_csv(self, month: str, save_path: Path) -> Path:`。
  - 直接写入用户选定的 `save_path`，写入成功后返回该路径。
  - 如遇写入异常（权限、磁盘满等），向上抛出，由 UI 层捕获。
- 导出成功后，弹出提示框告知用户文件的具体保存路径。

### 3.7 应用内装饰：动态蜗牛 (Interactive Snail)
- **图片处理要求**：使用 `snail.png`。保留 GGB 导出的网格线。只需将浅蓝色线条强制加深为深蓝色（如 `#1E3A8A`），使其清晰可见。蜗牛尺寸保持在 40x40 像素左右。
- **【UI修复】**：蜗牛线条必须强制加深为深蓝色，必须取消透明度，恢复到 100% 不透明。确保它在窗口顶部右侧清晰可见。
- **位置要求**：蜗牛作为独立小组件，直接悬浮在主窗口（`self`）的最顶层，用 `lift()` 确保不被其他控件遮挡。垂直位置固定在窗口顶部（例如 y=10）。
- **循环卷轴动画效果**：
  - **起点**：蜗牛从窗口顶部的右侧边缘出发（x = 窗口宽度）。
  - **向左爬行**：每次心跳让 x 坐标减去 2~3 像素。
  - **传送门逻辑 1（标题文字边缘）**：当蜗牛爬行到 `AccountKeeper` 标题文字的右侧边缘时，瞬间传送到标题文字的左侧边缘，继续向左爬行（不能从标题文字上穿过去）。
  - **传送门逻辑 2（屏幕边缘）**：当蜗牛爬出 GUI 窗口的最左侧边缘（x < 0）时，瞬间传送到窗口最右侧（x = 窗口宽度），重新开始向左爬行。
- **点击交互（对话泡泡）**： 
  - **【核心改造 · 弃用独立窗口】**：放弃 `tk.Toplevel`、`overrideredirect(True)`、`-transparentcolor` 等独立窗口方案。**改用主窗口内的局部 `tk.Canvas` 来绘制气泡**，彻底解决独立窗口跟随主窗口移动时的延迟问题。由于 Canvas 是主窗口的子控件，主窗口移动或缩放时，气泡会自动跟随，无需再写任何“跟随窗口移动”的同步逻辑。
  - **底层实现**：在 `_show_speech_bubble` 中，动态计算气泡所需宽高，然后创建一个 `tk.Canvas(self, width=..., height=..., bg="#F0F4F8", highlightthickness=0)`，作为主窗口的子控件。无需使用透明色键，不会出现灰底方块。
  - **绘制圆角与尾巴**：在该 Canvas 上，用 `create_polygon` 绘制平滑的圆角矩形作为气泡主体（填充色 `#FFF7D8`，无边框）。并在气泡底部绘制一个指向蜗牛中心的大三角形尾巴（宽度约16px，高约12px），颜色与主体一致。
  - **文字**：使用 `canvas.create_text` 绘制文字，字体为 `("Microsoft YaHei UI", 11)`，颜色 `#3E4A5A`，居中显示。
  - **尺寸自适应**：气泡大小必须根据文字实际长度动态计算，不要固定宽高，不要留大片空白。
  - **精准定位**：直接使用蜗牛的相对坐标（`self.snail_label.winfo_x()`、`winfo_y()`、`winfo_width()`、`winfo_height()`）来计算气泡位置。气泡底部尾巴尖端应正对蜗牛绝对中心点，气泡整体显示在蜗牛正上方 10 像素处。
  - **【核心修复 · 连点不卡死】**：每次点击蜗牛，必须先取消上一个尚未触发的 `after` 定时器，再销毁旧气泡 Canvas（调用 `destroy()` 并置 `self._bubble_canvas = None`），然后创建新气泡 Canvas。用一个属性 `self._bubble_after_id` 保存定时器的返回值，用于取消。销毁气泡时，必须确保 `self.snail_paused` 被重置为 `False`。整个点击处理逻辑要保证幂等：无论用户连点多少次，蜗牛最终都能恢复到爬行状态。
  - **暂停移动**：点击蜗牛时，必须设置 `self.snail_paused = True` 暂停爬行动画（在 `_animate_snail` 里加判断拦截，但保持 `after` 循环不中断）。气泡消失后设为 `False`。
  - **自动消失**：泡泡显示 3 秒后自动销毁。如果用户连续点击，先取消旧定时器、销毁旧泡泡，再创建新泡泡并重新计时。
  - **点击销毁**：点击气泡本身立刻销毁气泡，并恢复蜗牛移动。
  - **消息读取**：继续读取 `snail_messages.json` 并随机抽取，如果出错显示默认提示语，不能崩溃。
  - **【核心清理】**：删除原有的 `_on_window_move`、`_reposition_bubble`、`_move_after_id` 以及 `<Configure>` 事件绑定，不再需要任何窗口移动的防抖逻辑。

### 3.8 应用图标 (Application Icon)
- 必须使用 `image/logo.ico` 作为整个应用的图标（窗口左上角和任务栏）。
- 在 `main()` 函数初始化 `AccountKeeperApp` 之后，调用 `iconbitmap` 加载该图标。
- **Windows 任务栏兼容**：为了防止任务栏把图标显示成 Python 默认图标，必须在设置图标前调用 `ctypes.windll.shell32.SetCurrentProcessExplicitAppUserModelID("AccountKeeper.App")`。
- **容错机制**：如果 `logo.ico` 文件丢失或格式损坏，程序不能崩溃，只需在控制台打印警告，并继续以默认图标运行。

### 3.9 搜索与筛选 (Search & Filter)
- **界面要求**：在顶部工具栏（按钮行）增加一个搜索输入框（`CTkEntry`），宽度约 200 像素。
- **【UI修复 · 核心】**：搜索框必须使用 `CustomTkinter` 的**原生占位符**参数 `placeholder_text="🔍 搜索日期/类别/备注"`。
  - **禁止**用 `tk.StringVar(value="...")` 手动往搜索框里塞提示文字。
  - **禁止**用 `FocusIn` / `FocusOut` 事件手动清空和恢复提示文字。
  - 原生占位符的行为：平时显示灰色提示文字，鼠标点进去自动消失，用户可以直接打字；输入内容被清空后，提示文字自动回来。
- **实时过滤逻辑**：
  - 绑定 `self.search_var.trace_add("write", self.filter_records)`，用户每敲一个字符，就实时刷新表格。
  - 过滤规则：只要记录的**日期、类别、备注**中任意一个包含搜索关键词，就显示该记录。
  - 搜索关键词不区分大小写（统一转小写比较）。
- **联动汇总**：过滤后的表格，顶部的汇总标签（总收入、总支出）也必须**只计算当前过滤出来的记录**，而不是全部记录。
- **清空恢复**：当搜索框被清空时，立刻恢复显示所有记录，汇总标签也恢复全量计算。
- **容错**：搜索时不能修改数据库，也不能报错，纯前端过滤。

### 3.10 图表可视化 (Chart Visualization)
- **依赖**：允许使用 `matplotlib` 库（已下载）。
- **触发方式**：在工具栏增加一个【查看图表】按钮。
- **弹窗逻辑**：
  - 点击按钮后，弹出输入框让用户输入月份（格式 `YYYY-MM`），复用现有的 `ask_month` 方法。
  - 如果该月无记录，弹出提示“该月没有记录”，不显示图表。
- **图表内容**：
  - 展示该月**各分类的收入与支出总额**（不能只看支出，收入也必须纳入统计）。
  - 推荐使用**柱状图**（`bar`）展示：X轴为类别，Y轴为金额，正数代表收入，负数代表支出（可用不同颜色区分，比如绿色收入、红色支出）。
  - 如果数据只包含收入或只包含支出，柱状图也应当能正常画出，不能因为某一项为0就拦截弹窗。
  - 如果该月没有任何记录（无论收支），才弹窗提示“该月没有记录，无法生成图表”，不显示图表。
  - 图表需支持中文显示（字体设为 `Microsoft YaHei`），标题为 `${月份} 收入与支出统计`。
- **新窗口展示**：
  - 图表必须显示在一个**独立的 `tk.Toplevel` 窗口**中，不能阻塞主界面。
  - 使用 `FigureCanvasTkAgg` 将 matplotlib 图表嵌入到 Tkinter 窗口中（不要用 `plt.show()`，否则会弹出独立的 matplotlib 窗口，风格不统一）。
  - 窗口大小约 700x550，居中显示在屏幕上。
- **容错机制**：
  - 如果 `matplotlib` 未安装，程序不能崩溃，弹窗提示“请先安装 matplotlib”。
  - 关闭图表窗口时，释放 canvas 资源，避免内存泄漏。
  - 如果某些分类的名称过长，要确保图表上的标签不重叠（可以适当旋转X轴标签）。

### 3.11 用户自定义存储路径（DB / CSV）
#### 目标
允许用户自己决定：
1. SQLite 数据库文件 `account.db` 存放在哪里；
2. CSV 文件默认导出/迁移目录在哪里。

#### 配置文件
- 新增 `settings.json`，默认位置为项目目录下的 `settings.json`。
- 结构示例：
```json
{
  "db_path": "D:/AccountKeeper/account.db",
  "csv_dir": "D:/AccountKeeper/csv"
}